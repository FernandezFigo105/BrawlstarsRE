# Movement area box (for visualization/fallback)
# Provided coordinates (top-left, bottom-right):
MOVEMENT_BOX_RECT = ((50, 655), (240, 830))
# Default movement starting point (fallback if own health bar is not detected)
DEFAULT_MOVEMENT_START = (150, 745)

# Attack area box (for visualization/fallback)
ATTACK_BOX_RECT = ((1325, 565), (1520, 735))
# Default attack starting point
DEFAULT_ATTACK_START = (1433, 655)

# Distance threshold (in pixels) for deciding when to attack
SHOOT_DISTANCE = 120
